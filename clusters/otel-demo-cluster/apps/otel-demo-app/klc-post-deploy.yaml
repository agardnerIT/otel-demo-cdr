apiVersion: lifecycle.keptn.sh/v1alpha1
kind: KeptnTaskDefinition
metadata:
  name: post-deploy-service-task
  namespace: otel-demo
spec:
  function:
    inline:
      code: |
        console.log(">> Post-Deployment service task has been executed");
        const headers = new Headers({ "x-klc-step": "post-deploy-test-new" });
        const res = await fetch("https://webhook.site/3fee1127-d56a-4ac9-96bf-aff86508e660", {
          headers,
        });
        const body = new Uint8Array(await res.arrayBuffer());
        console.log(body)